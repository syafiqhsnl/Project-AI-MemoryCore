# 2026-04-23

## Session Overview
**Focus**: n8n GTFS ZIP extraction, GitHub Actions automated deployment, and n8n Performance Bottleneck analysis.
**Status**: Paused - Awaiting next session to apply performance fixes.

## Key Accomplishments
1. **GTFS ZIP Pipeline Fixed**: Bypassed the n8n distroless container `bestzip` limitations. Switched entirely to the native n8n Compression Node. Built a custom JS Code Node to dynamically intercept, rename, and merge 9 binary CSVs into a single output item, resulting in perfect, uncorrupted GTFS ZIP compilations without OS dependencies.
2. **Headless Deployment Resolved**: Fixed silent `git push` authentication failures that were occurring in the DO VPS n8n SSH nodes. Migrated the repository remote URL from HTTPS to SSH Deploy Keys (or PAT), allowing fully unattended GitHub Action triggers.
3. **Performance Diagnostics**: Investigated a massive regression where GTFS PROD workflows jumped from taking 15 seconds (on the old SQLite local setup) to 1 hour (on the DO Postgres setup). 

## Technical Root Causes Identified
- **The 1-Hour Bottleneck**: Triggered by a combination of enabling 'Save Data for Successful Executions' on massive arrays of data (forcing Postgres to write gigabytes of JSON logs) and the added TCP network handshake latency to the remote MySQL database.

## System Updates (Forge)
- Created a permanent knowledge entry in `library/workflow/n8n-multi-binary-compression.md` to codify the JS multi-binary merge pattern, headless git deployment rules, and execution throttling warnings.

## Next Session (Where to Resume)
- **Immediate Task**: The user has confirmed that `GTFS RND` finished and also took 1 hour. In the next session, we will turn off Execution Saving and apply the necessary fixes to both `GTFS RND` and `GTFS PROD` to bring the execution time back down.
